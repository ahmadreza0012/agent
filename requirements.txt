numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
pycoingecko>=3.1
yfinance>=0.2.36
groq>=0.9        # free tier LLM (https://console.groq.com) - optional, mock mode used if absent
PyPortfolioOpt>=1.5   # optional, scipy fallback is used automatically if missing
matplotlib>=3.7  # optional, only for plot_results()
flask>=2.3       # web server for Railway deployment
gunicorn>=21.0   # production WSGI server for Railway
requests>=2.31   # HTTP library for API calls
