# یادداشت‌های ارتقا (Upgrade Notes) — نسخه v2

این سند خلاصه‌ی صادقانه‌ی تغییرات، دلیل هرکدام، و **محدودیت‌هایی که همچنان باقی‌ست** را توضیح می‌دهد.

## 🔄 آپدیت: نتیجه‌ی تست یکپارچگی واقعی (integration test)

بعد از دور اول، `data_fetcher.py` اصلی رو هم اضافه کردم و کل سیستم را (`main.py` واقعی، نه یک تکه‌ی جدا) با یک منبع داده‌ی synthetic جایگزین Binance اجرا کردم تا خود زنجیره را — نه فقط تک‌تک فایل‌ها را — تست کنم. این کار **۳ باگ واقعی اضافه** پیدا کرد که رفع شدند:

1. **ناسازگاری نام نمادها**: `data_fetcher.py` اصلی (بدون تغییر) ستون‌ها را با یک آندرلاین اضافی می‌سازد (`'BTC_'` نه `'BTC'`، چون `'BTC/USDT'.replace('/','_').replace('USDT','')` این‌طور خروجی می‌دهد). این باعث می‌شد `ai_sentiment.py` نتواند نماد را با دیکشنری اخبار/کلیدواژه تطبیق دهد. → یک تابع نرمال‌سازی نماد (`_normalize_symbol`) اضافه شد.
2. **انتشار خاموش NaN در Black-Litterman**: وقتی طول lookback دقیقاً برابر پنجره‌ی ۱۶۸ساعته‌ی محاسبه‌ی سنتیمنت بود، `pct_change(168)` مقدار NaN می‌داد که تا انتهای بهینه‌سازی BL می‌رفت و بی‌سروصدا وزن‌ها را equal-weight می‌کرد. → `fillna(0)` + هشدار صریح در لاگ اضافه شد.
3. **سنتیمنت همیشه صفر با تنظیمات پیش‌فرض**: چون `lookback_hours` پیش‌فرض در `main.py` (۱۶۸) دقیقاً برابر پنجره‌ی سنتیمنت (۱۶۸) بود، حتی بعد از رفع باگ ۲، `Q` همیشه `[0,0,0,0,0]` می‌شد (بدون حاشیه‌ی امن داده). → `lookback_hours` به ۲۱۶ (۹ روز) افزایش یافت تا حاشیه‌ی امن باشد.

بعد از این ۳ رفع، پایپ‌لاین کامل (فچ داده → سنتیمنت → بهینه‌سازی چهارگانه → انتخاب خودکار استراتژی → بک‌تست walk-forward → ارزیابی) روی داده‌ی synthetic یک‌ساله **بدون کرش و بدون NaN اجرا شد**، و لاگ‌ها نشان دادند انتخاب‌گر استراتژی واقعاً بین `black_litterman`، `mvo` و `ml` در فولدهای مختلف جابه‌جا می‌شود — یعنی منطق «انتخاب خودکار استراتژی مناسب» واقعاً کار می‌کند، نه فقط روی کاغذ.

**نکته‌ی مهم دیگر که از این تست به دست آمد**: با `since_days=365` و `n_folds=4`، فقط ۲ ماه تقویمی کامل در کل مشاهده شد (چون هر fold ~۹۱ روز است و ۳۰٪ آن به‌عنوان تست جدا می‌شود ≈ ۲۷ روز، که اغلب کمتر از یک ماه تقویمی کامل است). **پیشنهاد**: برای نتیجه‌ی آماری قابل‌اتکاتر، یا `since_days` را به ۲-۳ سال افزایش دهید، یا `n_folds` را کم کنید (مثلاً ۲) تا هر fold طولانی‌تر شود.

⚠️ همچنان صادقانه بگویم: این تست با داده‌ی **synthetic (random walk)** انجام شد، نه داده‌ی واقعی بازار، و در حالت mock سنتیمنت (بدون Groq واقعی) بود — چون این سندباکس شبکه ندارد. عدد ۱.۷٪ بازده ماهانه که در این تست دیدید **کاملاً بی‌معنی است** و فقط برای اثبات «کرش نمی‌کند» است، نه ارزیابی عملکرد. باید همین را با داده‌ی واقعی و (اختیاری) کلید Groq روی سیستم خودتان اجرا کنید.

## ⚠️ مهم‌ترین نکته قبل از هر چیز

این فایل‌ها در محیطی نوشته و تست منطقی شدند که **دسترسی شبکه نداشت**. یعنی:
- منطق هر ماژول با داده‌ی synthetic/آفلاین تست و تأیید شد (بخش `if __name__ == "__main__"` هر فایل).
- اتصال واقعی به Binance، فیدهای RSS خبری، و Groq API **روی سیستم شما و توسط خودتان باید تست شود** — من نمی‌توانم صحت این اتصالات را از اینجا تضمین کنم.
- به هیچ عددی (بازده، شارپ و...) که از اجرای واقعی به دست نیامده اعتماد نکنید.

## چه چیزهایی درست شد

### ۱. باگ CVaR (`portfolio_optimizer.py`)
نسخه‌ی قبلی قید پیونددهنده‌ی متغیر کمکی `u` به بازده واقعی پرتفوی را نداشت (طبق فرمول Rockafellar-Uryasev باید `u_t ≥ -(returns_t · w) - z` باشد). بدون این قید، بهینه‌ساز می‌توانست `u=0` بگذارد و وزن‌های خروجی عملاً بی‌ربط به CVaR واقعی بودند. الان با یک **برنامه‌ریزی خطی (linprog, method='highs')** درست پیاده‌سازی شده — هم صحیح‌تر و هم سریع‌تر از SLSQP قبلی.

### ۲. اخبار واقعی (`news_fetcher.py` - جدید)
ماژول جدید از فیدهای RSS رایگان و بدون نیاز به API key (CoinDesk، Cointelegraph، CryptoPanic RSS عمومی) خبر می‌گیرد و بر اساس نماد فیلتر می‌کند. یک fallback شفاف مبتنی بر کلیدواژه هم دارد برای زمانی که LLM در دسترس نیست (این fallback با برچسب واضح "keyword fallback" علامت‌گذاری شده، جای خودش را به‌عنوان AI جا نمی‌زند).

### ۳. اتصال واقعی AI رایگان (`ai_sentiment.py`)
- دیگر `use_mock=True` در `main.py` هاردکد نیست؛ به‌صورت خودکار بر اساس وجود `GROQ_API_KEY` تصمیم می‌گیرد.
- در حالت واقعی، تیترهای خبری واقعی (نه حدس بدون context) به مدل Groq (`llama-3.1-8b-instant`, رایگان) داده می‌شود.
- یک مکانیزم ساده‌ی **خودتصحیحی (self-correcting)** اضافه شد: `record_outcome()` نتیجه‌ی واقعی هر پیش‌بینی جهت را ثبت می‌کند و `get_symbol_confidence()` بر اساس نرخ موفقیت اخیر هر نماد، اطمینان (omega در Black-Litterman) را خودکار تنظیم می‌کند — هرچه AI برای یک کوین بیشتر اشتباه کند، وزنش در تصمیم‌گیری کمتر می‌شود.

### ۴. انتخاب خودکار استراتژی (`strategy_selector.py` - جدید)
یک انتخاب‌گر که در هر rebalance:
- رژیم بازار (trending / mean_reverting / high_vol) را با یک فرمول ساده و شفاف تشخیص می‌دهد،
- امتیاز درون‌نمونه‌ای هر استراتژی را می‌سنجد،
- و از رکورد واقعی عملکرد گذشته‌ی هر استراتژی (وقتی واقعاً انتخاب شده بود) به‌عنوان سیگنال خودتصحیحی استفاده می‌کند.

این عمداً یک انتخاب‌گر شفاف و قابل ممیزی است، **نه یک عامل یادگیری تقویتی (RL) جعبه‌سیاه** — برای سیستمی که قرار است با سرمایه‌ی واقعی کار کند، توضیح‌پذیری مهم‌تر از پیچیدگی است. اگر می‌خواهید نسخه‌ی RL هم بسازیم، ممکن است ولی نیاز به داده‌ی بسیار بیشتر و ریسک overfitting بالاتری دارد.

### ۵. بک‌تست واقع‌بینانه‌تر (`backtester.py`)
- **رفع محاسبه‌ی گمراه‌کننده‌ی بازده ماهانه**: قبلاً از فرمول CAGR روی کل بازه‌ی تست استخراج می‌شد؛ الان از **بازده‌ی واقعی ماه‌به‌ماه تقویمی** محاسبه می‌شود (میانگین، میانه، و بدترین ماه هرکدام گزارش می‌شوند).
- **Walk-forward واقعی** به‌جای یک split ثابت ۷۵/۲۵ — چند fold متوالی، هرکدام train/test مستقل، تا نتیجه به یک بازه‌ی سه‌ماهه‌ی شانسی/بدشانسی وابسته نباشد.
- **مدار قطع ریسک (circuit breaker)**: اگر افت سرمایه از ۱۵٪ عبور کند، اکسپوژر پرتفوی خودکار نصف می‌شود تا ریکاوری.

### ۶. `main.py`
همه‌ی موارد بالا به هم متصل شدند؛ ارزیابی نهایی الان با صداقت کامل نشان می‌دهد اگر کمتر از ۶ ماه تقویمی داده‌ی out-of-sample مشاهده شده باشد، هشدار می‌دهد که نتیجه هنوز آماری قابل‌اتکا نیست.

## چیزهایی که عمداً اضافه نکردم (و چرا)

- **اجرای زنده/لایو تریدینگ روی صرافی واقعی**: خواسته نشده بود صراحتاً و ریسک مالی مستقیم دارد؛ پیشنهاد می‌کنم قبل از این مرحله حتماً چند ماه paper-trading انجام شود.
- **مدل یادگیری تقویتی (RL) کامل**: به دلایل بالا (شفافیت، ریسک overfit، نیاز داده‌ی بسیار زیاد).
- **هدف ۵٪ ماهانه تضمینی**: این هدف را نمی‌توان مهندسی کرد؛ فقط می‌توان زیرساخت درستی ساخت که *اگر* چنین ادجی در بازار وجود داشته باشد، بتواند آن را بگیرد. صداقتاً، با توجه به رقابتی‌بودن بازار کریپتو، این هدف بسیار جاه‌طلبانه است و نتیجه‌ی بک‌تست walk-forward واقعی (که باید خودتان روی سیستم خودتان اجرا کنید) تنها معیار قابل‌اتکا است.

## نحوه‌ی اجرا روی سیستم خودتان

```bash
pip install -r requirements.txt
export GROQ_API_KEY="your_free_groq_key"   # اختیاری؛ رایگان از https://console.groq.com
python main.py
```

بدون `GROQ_API_KEY`، سیستم خودکار به حالت mock (مبتنی بر مومنتوم قیمت، نه خبر واقعی) سوییچ می‌کند و این را با هشدار صریح در لاگ اعلام می‌کند.

## دستورات Git برای پوش به مخزن خودتان

```bash
# در پوشه‌ی محلی کلون‌شده‌ی پروژه:
git checkout -b upgrade/real-ai-cvar-fix-walkforward

# فایل‌های جدید/جایگزین‌شده را کپی کنید روی پروژه، سپس:
git add news_fetcher.py ai_sentiment.py portfolio_optimizer.py \
        strategy_selector.py backtester.py main.py requirements.txt UPGRADE_NOTES.md

git commit -m "fix: correct CVaR LP formulation; add real news+LLM sentiment,
self-correcting confidence, automatic regime-based strategy selection,
walk-forward backtesting with real calendar-month returns and drawdown
circuit breaker"

git push origin upgrade/real-ai-cvar-fix-walkforward
# سپس روی گیت‌هاب یک Pull Request به main باز کنید.
```

## فایل‌های تغییر یافته/جدید در این ارتقا

| فایل | وضعیت |
|---|---|
| `news_fetcher.py` | جدید |
| `strategy_selector.py` | جدید |
| `ai_sentiment.py` | بازنویسی کامل |
| `portfolio_optimizer.py` | بازنویسی (رفع باگ CVaR) |
| `backtester.py` | بازنویسی (walk-forward + circuit breaker) |
| `main.py` | بازنویسی (اتصال همه‌ی اجزا) |
| `requirements.txt` | به‌روزرسانی |
| `data_fetcher.py` | بدون تغییر (این بخش از ابتدا درست بود) |
| `grid_search.py`, `docs/`, `tests/` | بررسی نشد — اگر بخواهید در دور بعد بررسی می‌کنم |
